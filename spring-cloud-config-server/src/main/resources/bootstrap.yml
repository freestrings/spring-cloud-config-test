info:
  component: Config Server
spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: file:${CONF_PATH}
#          uri: git@github.com:freestrings/config-repo.git
server:
  port: 8888